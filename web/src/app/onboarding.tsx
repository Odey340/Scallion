import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function OnboardingScreen() {
  return (
    <PlaceholderScreen
      title="Onboarding"
      description="What leaves your phone; the medication question; the two LSNS questions; optional family tag on top contacts; Persona hosted link only on the sharing step."
      contractRef="Block 4 — docs/lanes/C.md"
    />
  );
}
