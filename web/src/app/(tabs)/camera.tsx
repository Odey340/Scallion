import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function CameraScreen() {
  return (
    <PlaceholderScreen
      title="Camera"
      description="30s capture with a signal-quality bar (Presage); pulse/breathing from /vitals/latest; perceived age via @vladmandic/human, +/-6 label; fitness age from resting HR."
      contractRef="Block 1 & 3 — docs/lanes/C.md, contracts.md §3"
    />
  );
}
