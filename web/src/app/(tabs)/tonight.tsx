import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function TonightScreen() {
  return (
    <PlaceholderScreen
      title="Tonight"
      description="Photograph the plate -> carbs from D's Gemini call -> two CurveBands from meal_grid.json (eat now vs plus a walk); the caffeine last-coffee line from caffeine.json."
      contractRef="Block 3 — docs/lanes/C.md, contracts.md §1"
    />
  );
}
