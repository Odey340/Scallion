import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function LabsScreen() {
  return (
    <PlaceholderScreen
      title="Labs"
      description="Upload -> review -> waterfall. pdf.js text-layer highlights on each extracted value's source_span, fasting-status question, hs-CRP flag, typed-entry fallback."
      contractRef="Block 2-3 — docs/lanes/C.md, contracts.md §1 §3"
    />
  );
}
