import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function HomeScreen() {
  return (
    <PlaceholderScreen
      title="Home"
      description="The clock (PhenoAge if labs, else fitness age) with its band; the levers ledger from risk_years.json; today's one nudge; distancing/active state."
      contractRef="Block 2 — docs/lanes/C.md"
    />
  );
}
