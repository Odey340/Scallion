import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function CoachScreen() {
  return (
    <PlaceholderScreen
      title="Coach"
      description="Push-to-talk to D's ElevenLabs agent, captions, text fallback, read-aloud button on every card."
      contractRef="Block 4 — docs/lanes/C.md, contracts.md §4"
    />
  );
}
