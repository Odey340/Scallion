Redacted samples only. Never commit an unredacted lab report or a raw chat export.

`lab_report_synthetic.pdf` is generated by `api/tests/make_fixture.py`: invented CBC + CMP + hs-CRP values, no identifiers, not a real patient. It stands in for `lab_report_redacted.pdf` until the consented one is added.
