# Lane D log

Append ten lines per session: done, blocked, next, contract changes needed.

## Session 1 (Fri, H0-H2)
1. Done: `api/` scaffold (FastAPI, pydantic-settings, Supabase HS256 JWT dependency with `DEV_AUTH_BYPASS`, CORS, Dockerfile, README), `GET /health` reporting gemini fake/live and auth jwt/bypass.
2. Done: `POST /extract`: Gemini 2.5 Flash with a strict pydantic response schema (raw_name, value, unit, ref range, source_text, fasting, lang); canonical mapping in Python from `phenoage.json` keys; spans located in the pypdf text layer; `FakeExtractor` for offline runs; upload held in memory only.
3. Done: `fixtures/lab_report_synthetic.pdf` (generated, no PHI) + canned Gemini response; 48 tests pass offline; curl run returns all nine analytes with spans.
4. Done: root `.env.example` with every key empty; `[contract]` v2 for the `/extract` additive fields (post in Discord).
5. Not verified: Docker build (no daemon in this sandbox); live Gemini call (no key here). Both are one command each in `api/README.md`.
6. Blocked: real `fixtures/lab_report_redacted.pdf` not in repo; `GEMINI_API_KEY` and `SUPABASE_JWT_SECRET` not in hand. H6 needs the real PDF through live Gemini.
7. Next: redaction rules JSON for C (Block 1 item 2); Tiger Data schema + Apple Health import; `presage-worker/` pulse.
8. Next: Block 2 unit normalizer (+ lymph % from absolutes, Spanish aliases) on top of `app/extract/canonical.py`.
9. Choices made without asking: `phenoage.json` fallback lives at `api/fixtures/phenoage.contract.json` (TODO(A)); a token with no `SUPABASE_JWT_SECRET` configured returns 503, not 401, so misconfiguration is visible; duplicate canonical rows (a second glucose) come back as `other:`.
10. Contract changes needed: none beyond v2 above.

## Session 2 (Sat)
1. Verified: Docker image builds and runs; `/health` -> `{"gemini":"fake","auth":"jwt"}` with `GEMINI_FAKE=1`, `/extract` without a token -> 401, and a bare container refuses to start without `GEMINI_API_KEY` (closed by default). Rebuilt after adding psycopg; the worker's fixture payload posts and reads back through the container.
2. Not verified: live Gemini `/extract` (no `.env` on this machine, so no `GEMINI_API_KEY`); `fixtures/lab_report_redacted.pdf` still not in the repo. H6 stays: engine numbers (A), Gmail headers (B), nine analytes on a real PDF (needs key + PDF), Presage pulse (needs key). Green so far: nine analytes from the synthetic PDF offline, Docker.
3. Done: `api/redaction_rules.json` (17 rules: name, DOB, MRN, address, ordering physician, contact; `keep_if` guard for result rows; JS/Python-safe regex), `app/redaction.py` reference applier, tests on the fixture text layer + three invented headers + Spanish labels. `[contract]` v3 section 7.
4. Done: `api/migrations/0001..0004` (contact_events hypertable + dedup index, daily_connection cagg + hourly policy, vitals, clock_history) and `migrations/apply.py --show` (autocommit, once per file). Not applied: no `TIGER_DATABASE_URL`. Integration tests skip without it.
5. Done: `presage-worker/` on `@smartspectra/node-sdk` 3.3.0 (headless `useCamera()`, `maxDurationMs` 30 s, median of the confident second half, Baevsky index as `stress_index`); `npm test` 6/6; `--dry-run` without a key exits 2. Not run on the webcam: no `PRESAGE_API_KEY`. Laptop camera is "HP 5MP Camera" (device 0).
6. Done: `POST /vitals`, `GET /vitals/latest` (memory store without the DB URL, Tiger store with it; `/health.db` says which). API suite: 83 passed, 4 skipped.
7. Blocked on the human: `GEMINI_API_KEY`, `TIGER_DATABASE_URL`, `PRESAGE_API_KEY` in `.env`; the consented redacted PDF. With those, the remaining checks are one command each (README).
8. Next: apply migrations + `pytest tests/test_tiger_integration.py`; `node index.mjs` on the webcam and screen-record it; Block 2 unit normalizer (+ lymph % from absolutes, Spanish aliases) on `app/extract/canonical.py`; Persona webhook + `/me`; ElevenLabs `GET /tts` for C's H10 test.
9. Choices made without asking: `DEV_USER_ID` changed to `00000000-0000-4000-8000-000000000001` (old value was not a valid uuid); vitals rows carry optional `hrv_rmssd_ms`, `confidence`, `samples` beyond the contract (additive, nullable); `SCALLION_API_URL`/`SCALLION_API_TOKEN` added to `.env.example`; Apple Health import deferred until the DB URL exists.
10. Contract changes needed: none beyond v3 (section 7). B may want `DELETE /events`; not added yet.
11. (later, H8) Live Gemini verified with the team key on `gemini-3.6-flash` (2.5-flash is retired for new keys): nine analytes, all spans resolve, fasting=true, lang=en, ~35 s. H6 "nine analytes" is green on the synthetic PDF; the real redacted PDF is still the missing piece. Migrations and webcam still wait on `TIGER_DATABASE_URL` and `PRESAGE_API_KEY`.
12. (H8) Presage pulse on the demo laptop: `node index.mjs` -> 68.5 bpm from 17 confident samples (conf 0.79), posted to the local API and read back from `/vitals/latest`. H6 "Presage prints a pulse" is green. Breathing came back null in that run (fewer than 3 confident entries in 30 s); stress/HRV null. SDK facts learned: it waits for a face before Running, streams metrics continuously, ignores maxDurationMs with useCamera(), confidence is 0..100. No screen recording yet (TODO media/presage_pulse.mp4).
13. Tiger Cloud: the console login's Google button is a cross-origin iframe the Chrome extension cannot click through; sign-in handed to the human. Migrations still pending the URL.
14. (H9) Tiger Data live: service `scallion` (id m6hmkz5jcx, project ut6hi0n8gu, AWS us-east-1, free shared tier, 1 GiB) created via the console; connection string is in `.env` only (never commit). Five migrations applied (`0005` turns on real-time aggregation: Tiger Cloud defaults caggs to materialized_only=true). `apply.py --show`: 3 hypertables + `daily_connection`. Integration tests green (88 passed with the URL; 84 + 4 skipped without). The real webcam reading (68.5 bpm) is row 1 of `vitals`; local API `/health` reports `db: tiger`.
15. Next: screen-record the worker + `/vitals/latest` for media/; unit normalizer (Block 2); Persona webhook + `/me`; ElevenLabs `GET /tts`; `POST /events` for B against `contact_events` (dedup index is there, use `on conflict do nothing`).
16. (H10) Block 2 done: unit normalizer (`si_value`/`si_unit` in the paper's units, unknown units flagged not guessed, lymph % derived from absolute/WBC and marked `derived`), 33 Spanish aliases with accent folding; contract v4. `POST /events` + `DELETE /events[/{contact}]` on `contact_events` (idempotent via the dedup index; raw handles rejected by the 64-hex check); contract v5. `GET /tts` (ElevenLabs multilingual v2 or fake MP3; no key in `.env` yet, so `/health.tts` is "off" live and "fake" in tests). Persona webhook (HMAC `Persona-Signature`, approved -> verified + birthdate by reference-id) + `GET /me` (`over_65`, `age`, `verify_url`) + `PUT /me/lang`; migration 0006 `profiles` applied; contract v6. 174 unit tests, 6 Tiger integration tests, all green.
17. Blocked on the human: `ELEVENLABS_API_KEY` (+ optional voice id) for a live sentence; `PERSONA_TEMPLATE_ID` / `PERSONA_WEBHOOK_SECRET` (sandbox, Government ID + selfie template) and a public URL for the webhook (Vultr deploy not started; ngrok is the H10 shortcut). No ElevenLabs agent yet.
18. Next (Block 3): `GET /coach/context` from A's exports + `/vitals/latest` + `/me` + a stub for B's `social/` (TODO(B)); the number validator; coach client tools; Vultr + domain deploy; Backboard seed. Then the Devpost draft.
19. (H11) Block 3: `GET /coach/context` (clock rows C posts via `POST /clock` with `delta_years`/`show`, latest vitals, A's caffeine rule for the last-coffee time, levers from `risk_years.json` conditions vs `PUT /me/answers`, flags: critical ranges hide the age, medication blocks exercise timing), `POST /coach/validate` (numbers not in the context are rejected; timestamps/citations excluded), `coach_tools.json` (six client tools + system prompt for the ElevenLabs agent). Migration 0007 applied. 185 unit + 7 Tiger tests green; live server on port 8000 (`--reload`) answers `/coach/context` from Tiger with the real 68.5 bpm row. Contract v7.
20. Still stubbed: `circle` (TODO(B) social/ package; `/circle/summary` not built), `explain_analyte` curated texts, `log_meal`/`share_with_circle` handlers, Backboard memory, the ElevenLabs agent itself (no key). Next: Vultr + domain deploy (Docker image is ready), the curated analyte explanations, Backboard seed, Devpost draft; then `GET /circle/summary` the moment B's package exists.
21. (H12) Deployed: Vultr `api.scallion.us` (104.238.145.198, Dallas, Ubuntu 24.04, 2 vCPU/4 GB) reinstalled through the API with cloud-init (deploy key `~/.ssh/scallion_vultr` on the demo laptop, public half in `api/deploy/scallion_deploy.pub`, Docker, ufw 22/80/443). `api/deploy/deploy.sh` ships api/ + engine JSON + `.env` and runs compose (API + Caddy TLS). GoDaddy `A api -> 104.238.145.198` is live. Box `/health`: gemini live, auth jwt, db tiger, tts off. `GET /coach/explain/{name}` (curated en/es analyte paragraphs, one citation each) and the Devpost draft (`docs/devpost.md`) landed while the box reinstalled. 189 unit tests.
22. Still needed from humans: `SUPABASE_JWT_SECRET` (C's logins against the deployed API), `ELEVENLABS_API_KEY`, Persona template/secret (webhook URL is now `https://api.scallion.us/persona/webhook`). Redeploy after any `.env` change: `bash api/deploy/deploy.sh`.
23. (H13) Secrets in: Supabase JWT secret (deployed `/me` 401 without / 200 with a real HS256 token), ElevenLabs (free tier; premade "Rachel" id is gone for new accounts, default voice is now "Sarah" `EXAVITQu4vr4xnSDxMaL`, multilingual v2 without `language_code`; EN and ES MP3s verified live), Persona webhook secret (signed `inquiry.approved` against `https://api.scallion.us/persona/webhook` sets verified + birthdate; bad signature 401). Persona's REST API rejects the sandbox key on every endpoint, so the template id (`itmpl_...`) must come from the dashboard. `/health` on the box: gemini live, auth jwt, db tiger, tts live.
24. (H13) Persona wired: template `itmpl_AH43ZFeBwqDRXTQLsUW8vEpdokrjAU` in `.env`, deployed; `/me` returns the hosted-flow `verify_url` with `reference-id=<user id>`. Webhook `wbh_AH43ZFeWmkCLd9pYYVvXfznZEinn6D` exists at `https://api.scallion.us/persona/webhook` but showed **Disabled** in the dashboard; it must be enabled before a real inquiry marks a user verified. H10 on D's side: deployed TLS API, JWT, live Gemini, live ElevenLabs, Persona link. C's phone test can start.
25. (H14) Block 4: coach memory (`checkins` table 0008, memory/Tiger stores; Backboard not wired, no key; the cut order's Postgres fallback is what ships), `POST /coach/meal` + `POST /coach/share` (403 unless verified), `GET /coach/session` (agent id + signed URL minted server-side), context `history`/`last_plan`/`today.meal`; ElevenLabs agent `agent_0201m2bqs2gff2j8qrrk5n7c1327` "Scallion coach" (six client tools, EN default, ES preset, voice Sarah, turbo v2 / v2.5) created via `scripts/elevenlabs_agent.py`; demo check-ins seeded for the dev user in Tiger (`scripts/seed_demo.py --user <uuid>`; seed the real demo Supabase uuid before the demo). `/extract.complete` (order guide, re-test date, fasting action; reference prices labelled). Contracts v8, v9. Deployed and verified with a real JWT: session, meal, share refusal, history. 199 unit + 8 Tiger tests.
26. Human tasks: enable is done for the Persona webhook; run one sandbox inquiry from the `/me` link on a phone (force-pass) to see `verified: true`; ElevenLabs free tier has ~15 min of agent conversation, keep rehearsals short; record media/ clips (worker pulse, /tts, Persona pass).
27. (H15) Backboard wired (key from Joseph): one Backboard assistant per user (`profiles.backboard_assistant_id`, migration 0009, created lazily), every check-in/meal/share mirrored as a memory in a background thread, `GET /coach/recall?q=` semantic search; `/health.memory` backboard|table. Verified live: "did I walk after dinner yesterday" returns the seeded plan and the check-in. Gotcha: Backboard's add-memory returns 500 for metadata, so the kind is in the text. Unit tests now blank `BACKBOARD_API_KEY` (they had created six stray assistants; deleted). 203 unit + 8 Tiger tests.
28. (H16) Block 5: `/extract` cache fallback keyed by upload sha256 (`api/fixtures/cache/`, built by `scripts/cache_extract.py` from a live run; served when Gemini fails or with `X-Scallion-Cache: prefer`; header `x-scallion-cache: hit|miss`), presage-worker `--replay test/fixtures/capture_real.json` (the real Saturday capture, 68.5 bpm, no camera needed), QR cards in `media/` (`qr_cards.html` printable), `docs/demo_runbook.md` (live steps, fallbacks, 20-second sponsor swap-ins, Q&A one-liners). 208 unit tests. Devpost draft in `docs/devpost.md`; submission itself is Sunday 8:30 with the video (human).
29. Not done by the agent: media GIFs/recordings (needs a human at the screen), the Devpost submit click, the redacted real PDF cache (run `scripts/cache_extract.py` on it once it exists; only commit if it is PHI-free).
30. (H22) Joseph's call: D built the circle stand-in. `GET /circle/summary` over `contact_events` with the section 2 thresholds (`app/circle/metrics.py`, TODO(B) to replace), wired into `/coach/context` (`circle`, `today.nudge`); `scripts/seed_events.py` seeds 90 synthetic days (12 hashed contacts, 3 fading). Verified on the box with a real token: 9 active / 7 close ties, 3 overdue nudges, 364-day heatmap. Typed web client `web/src/lib/api.ts` for C (zero tsc errors; C's own files have pre-existing ones). Contract v10.
31. Judged demo account: fixed uuid `5ca11100-0000-4000-8000-000000000001`, 60 h HS256 token in `.env` as `EXPO_PUBLIC_DEMO_TOKEN` (also `DEMO_USER_ID`); seeded events + check-ins + answers in Tiger. Put `EXPO_PUBLIC_DEMO_TOKEN` and `EXPO_PUBLIC_API_URL=https://api.scallion.us` into Vercel env for the web export. Deploy bug found and fixed: compose did not mount `web/public/engine` into the container (caffeine/levers were empty on the box); now a read-only volume.
32. scallion.us status check (Joseph asked): DNS at Porkbun, apex -> Vercel 200, bundle loads, no console errors; `/start` works, Home/Labs/Circle/Coach are C's placeholder screens; `www` TLS fails (domain not added in the Vercel project). Lane B: 0 commits, no social/.
33. (Sat night, by the Lane C agent at Joseph's request) Persona hosted flow showed "This application is misconfigured / could not load template": the template link carries no `environment-id` and the sandbox template is not found in production. Fix in `app/routes/persona.py`: with `PERSONA_API_KEY` set, `/me` creates the inquiry server-side (reference-id = user id, so the existing webhook matching is unchanged) and returns Persona's hosted one-time link (`POST /inquiries` + `generate-one-time-link`), cached per user for an hour; any failure falls back to the template link (+ `environment-id` when set). `persona_api_key` added to Settings; tests blank it like Backboard's; two tests with an httpx MockTransport. 216 passed. The sandbox key Joseph created has inquiry read/write but not template read (`/inquiry-templates` answers "must be authenticated"); that is enough. Rotate the key after the event (it was pasted in chat).
34. (Sun early, Joseph's report: "invalid token: InvalidAlgorithmError. Type the values from the report below" on a real lab photo) Root cause: the Supabase project signs with ES256 by default; C moved the legacy HS256 key to in_use (log C session 15) but a browser session established before that rotation keeps presenting its ES256 access token (the store's `refreshSession()` error is swallowed, so a failed refresh keeps the old token), and `api/app/auth.py` only accepted HS256. Fix (the TODO(D) from C's log): the API now reads the token's `alg` and verifies HS256 against `SUPABASE_JWT_SECRET` and any asymmetric alg (ES256 first) against `<SUPABASE_URL>/auth/v1/.well-known/jwks.json` via PyJWKClient (cached, refetches on an unknown kid, so rotating back to ES256 needs no redeploy). `SUPABASE_URL` added to Settings and `.env`; `/health.auth` says `jwt+jwks`. 7 new tests (mocked JWKS, unknown kid, wrong aud, JWKS-only config); 223 passed. Deployed to the box: real kid resolves, a forged ES256 token with the real kid is rejected with InvalidSignatureError, the demo HS256 token is 200. Not verified with a genuine pre-rotation ES256 session (none in hand); Joseph should reload the app and retry the upload. Note for C: `auth-store.ts` should log/handle the `refreshSession()` error instead of dropping it.
35. (Sun H28, camera screen "No reading arrived") Two causes. (a) The worker POSTs under `SCALLION_API_TOKEN`, and the camera screen reads `/vitals/latest` for the signed-in user; `.env` pointed at localhost with no token. Now `SCALLION_API_URL=https://api.scallion.us` and a 60 h HS256 token minted for Joseph's Outlook account (`da7d36e7-...`); re-mint for another account with the same PyJWT one-liner (secret = `SUPABASE_JWT_SECRET`, aud `authenticated`). (b) The worker hung after recording on Windows: `sdk.stop()` is the SDK's blocking native Stop() and with a live camera it never returned (no timer fired for 80+ s), and `sdk.destroy()` blocks the same way. `src/capture.mjs` now calls `stopAsync()` (koffi worker thread), resolves on Idle or a 10 s grace, skips destroy(), and `index.mjs` ends with `process.exit(0)` after the POST. Verified live: 30 s capture -> Idle at 37 s -> 72.6 bpm (conf 0.75, n=10) -> POST 200 -> row on `/vitals/latest`. `--replay test/fixtures/capture_real.json` also posts (68.5 bpm) for a no-camera demo. 7 worker tests green. Demo timing: the screen accepts a row captured up to 60 s before Start, so run the worker and press Start within a minute (or press Start first; capture + post takes ~40 s, the screen polls for 55 s).
