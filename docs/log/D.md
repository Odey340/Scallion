# Lane D log

Append ten lines per session: done, blocked, next, contract changes needed.

## Session 1 (Fri, H0-H2)
1. Done: `api/` scaffold (FastAPI, pydantic-settings, Supabase HS256 JWT dependency with `DEV_AUTH_BYPASS`, CORS, Dockerfile, README), `GET /health` reporting gemini fake/live and auth jwt/bypass.
2. Done: `POST /extract`: Gemini 2.5 Flash with a strict pydantic response schema (raw_name, value, unit, ref range, source_text, fasting, lang); canonical mapping in Python from `phenoage.json` keys; spans located in the pypdf text layer; `FakeExtractor` for offline runs; upload held in memory only.
3. Done: `fixtures/lab_report_synthetic.pdf` (generated, no PHI) + canned Gemini response; 48 tests pass offline; curl run returns all nine analytes with spans.
4. Done: root `.env.example` with every key empty; `[contract]` v2 for the `/extract` additive fields (post in Discord).
5. Not verified: Docker build (no daemon in this sandbox); live Gemini call (no key here). Both are one command each in `api/README.md`.
6. Blocked: real `fixtures/lab_report_redacted.pdf` not in repo; `GEMINI_API_KEY` and `SUPABASE_JWT_SECRET` not in hand. H6 needs the real PDF through live Gemini.
7. Next: redaction rules JSON for C (Block 1 item 2); Tiger Data schema + Apple Health import; `presage-worker/` pulse.
8. Next: Block 2 unit normalizer (+ lymph % from absolutes, Spanish aliases) on top of `app/extract/canonical.py`.
9. Choices made without asking: `phenoage.json` fallback lives at `api/fixtures/phenoage.contract.json` (TODO(A)); a token with no `SUPABASE_JWT_SECRET` configured returns 503, not 401, so misconfiguration is visible; duplicate canonical rows (a second glucose) come back as `other:`.
10. Contract changes needed: none beyond v2 above.

## Session 2 (Sat)
1. Verified: Docker image builds and runs; `/health` -> `{"gemini":"fake","auth":"jwt"}` with `GEMINI_FAKE=1`, `/extract` without a token -> 401, and a bare container refuses to start without `GEMINI_API_KEY` (closed by default). Rebuilt after adding psycopg; the worker's fixture payload posts and reads back through the container.
2. Not verified: live Gemini `/extract` (no `.env` on this machine, so no `GEMINI_API_KEY`); `fixtures/lab_report_redacted.pdf` still not in the repo. H6 stays: engine numbers (A), Gmail headers (B), nine analytes on a real PDF (needs key + PDF), Presage pulse (needs key). Green so far: nine analytes from the synthetic PDF offline, Docker.
3. Done: `api/redaction_rules.json` (17 rules: name, DOB, MRN, address, ordering physician, contact; `keep_if` guard for result rows; JS/Python-safe regex), `app/redaction.py` reference applier, tests on the fixture text layer + three invented headers + Spanish labels. `[contract]` v3 section 7.
4. Done: `api/migrations/0001..0004` (contact_events hypertable + dedup index, daily_connection cagg + hourly policy, vitals, clock_history) and `migrations/apply.py --show` (autocommit, once per file). Not applied: no `TIGER_DATABASE_URL`. Integration tests skip without it.
5. Done: `presage-worker/` on `@smartspectra/node-sdk` 3.3.0 (headless `useCamera()`, `maxDurationMs` 30 s, median of the confident second half, Baevsky index as `stress_index`); `npm test` 6/6; `--dry-run` without a key exits 2. Not run on the webcam: no `PRESAGE_API_KEY`. Laptop camera is "HP 5MP Camera" (device 0).
6. Done: `POST /vitals`, `GET /vitals/latest` (memory store without the DB URL, Tiger store with it; `/health.db` says which). API suite: 83 passed, 4 skipped.
7. Blocked on the human: `GEMINI_API_KEY`, `TIGER_DATABASE_URL`, `PRESAGE_API_KEY` in `.env`; the consented redacted PDF. With those, the remaining checks are one command each (README).
8. Next: apply migrations + `pytest tests/test_tiger_integration.py`; `node index.mjs` on the webcam and screen-record it; Block 2 unit normalizer (+ lymph % from absolutes, Spanish aliases) on `app/extract/canonical.py`; Persona webhook + `/me`; ElevenLabs `GET /tts` for C's H10 test.
9. Choices made without asking: `DEV_USER_ID` changed to `00000000-0000-4000-8000-000000000001` (old value was not a valid uuid); vitals rows carry optional `hrv_rmssd_ms`, `confidence`, `samples` beyond the contract (additive, nullable); `SCALLION_API_URL`/`SCALLION_API_TOKEN` added to `.env.example`; Apple Health import deferred until the DB URL exists.
10. Contract changes needed: none beyond v3 (section 7). B may want `DELETE /events`; not added yet.
11. (later, H8) Live Gemini verified with the team key on `gemini-3.6-flash` (2.5-flash is retired for new keys): nine analytes, all spans resolve, fasting=true, lang=en, ~35 s. H6 "nine analytes" is green on the synthetic PDF; the real redacted PDF is still the missing piece. Migrations and webcam still wait on `TIGER_DATABASE_URL` and `PRESAGE_API_KEY`.
12. (H8) Presage pulse on the demo laptop: `node index.mjs` -> 68.5 bpm from 17 confident samples (conf 0.79), posted to the local API and read back from `/vitals/latest`. H6 "Presage prints a pulse" is green. Breathing came back null in that run (fewer than 3 confident entries in 30 s); stress/HRV null. SDK facts learned: it waits for a face before Running, streams metrics continuously, ignores maxDurationMs with useCamera(), confidence is 0..100. No screen recording yet (TODO media/presage_pulse.mp4).
13. Tiger Cloud: the console login's Google button is a cross-origin iframe the Chrome extension cannot click through; sign-in handed to the human. Migrations still pending the URL.
14. (H9) Tiger Data live: service `scallion` (id m6hmkz5jcx, project ut6hi0n8gu, AWS us-east-1, free shared tier, 1 GiB) created via the console; connection string is in `.env` only (never commit). Five migrations applied (`0005` turns on real-time aggregation: Tiger Cloud defaults caggs to materialized_only=true). `apply.py --show`: 3 hypertables + `daily_connection`. Integration tests green (88 passed with the URL; 84 + 4 skipped without). The real webcam reading (68.5 bpm) is row 1 of `vitals`; local API `/health` reports `db: tiger`.
15. Next: screen-record the worker + `/vitals/latest` for media/; unit normalizer (Block 2); Persona webhook + `/me`; ElevenLabs `GET /tts`; `POST /events` for B against `contact_events` (dedup index is there, use `on conflict do nothing`).
16. (H10) Block 2 done: unit normalizer (`si_value`/`si_unit` in the paper's units, unknown units flagged not guessed, lymph % derived from absolute/WBC and marked `derived`), 33 Spanish aliases with accent folding; contract v4. `POST /events` + `DELETE /events[/{contact}]` on `contact_events` (idempotent via the dedup index; raw handles rejected by the 64-hex check); contract v5. `GET /tts` (ElevenLabs multilingual v2 or fake MP3; no key in `.env` yet, so `/health.tts` is "off" live and "fake" in tests). Persona webhook (HMAC `Persona-Signature`, approved -> verified + birthdate by reference-id) + `GET /me` (`over_65`, `age`, `verify_url`) + `PUT /me/lang`; migration 0006 `profiles` applied; contract v6. 174 unit tests, 6 Tiger integration tests, all green.
17. Blocked on the human: `ELEVENLABS_API_KEY` (+ optional voice id) for a live sentence; `PERSONA_TEMPLATE_ID` / `PERSONA_WEBHOOK_SECRET` (sandbox, Government ID + selfie template) and a public URL for the webhook (Vultr deploy not started; ngrok is the H10 shortcut). No ElevenLabs agent yet.
18. Next (Block 3): `GET /coach/context` from A's exports + `/vitals/latest` + `/me` + a stub for B's `social/` (TODO(B)); the number validator; coach client tools; Vultr + domain deploy; Backboard seed. Then the Devpost draft.
19. (H11) Block 3: `GET /coach/context` (clock rows C posts via `POST /clock` with `delta_years`/`show`, latest vitals, A's caffeine rule for the last-coffee time, levers from `risk_years.json` conditions vs `PUT /me/answers`, flags: critical ranges hide the age, medication blocks exercise timing), `POST /coach/validate` (numbers not in the context are rejected; timestamps/citations excluded), `coach_tools.json` (six client tools + system prompt for the ElevenLabs agent). Migration 0007 applied. 185 unit + 7 Tiger tests green; live server on port 8000 (`--reload`) answers `/coach/context` from Tiger with the real 68.5 bpm row. Contract v7.
20. Still stubbed: `circle` (TODO(B) social/ package; `/circle/summary` not built), `explain_analyte` curated texts, `log_meal`/`share_with_circle` handlers, Backboard memory, the ElevenLabs agent itself (no key). Next: Vultr + domain deploy (Docker image is ready), the curated analyte explanations, Backboard seed, Devpost draft; then `GET /circle/summary` the moment B's package exists.
